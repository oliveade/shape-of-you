<nav class="bg-gradient-to-r from-rose-400 to-red-400 text-white shadow-md">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex justify-between h-16">
            <div class="flex items-center">
                <a href="{{ path('app_home') }}" class="text-xl font-bold">Shape of You</a>
            </div>
            <div class="hidden md:flex space-x-4 items-center">
                <a href="{{ path('app_home') }}" class="hover:text-gray-600">Accueil</a>
                {% if not app.user %}
                    <a href="{{ path('app_login') }}" class="hover:text-gray-600">Connexion</a>
                    <a href="{{ path('app_register') }}" class="hover:text-gray-600">Inscription</a>
                {% else %}
                    <a href="{{ path('app_profile') }}" class="hover:text-gray-600">Profil</a>
                    {% if is_granted('ROLE_ADMIN') %}
                        <a href="{{ path('app_admin') }}" class="hover:text-gray-600">Admin</a>
                    {% endif %}
                    <a href="{{ path('app_logout') }}" class="hover:text-gray-600">Déconnexion</a>
                {% endif %}
            </div>
            <div class="md:hidden flex items-center">
                <button id="menu-button" class="focus:outline-none">
                    <img src="/img/burger.png" alt="Menu" class="w-6 h-6">
                </button>
            </div>
        </div>
    </div>
    <div id="mobile-menu" class="hidden md:hidden bg-gradient-to-r from-rose-400 to-red-400 text-white text-center space-y-2 py-2 px-4 shadow-lg">
        <a href="{{ path('app_home') }}" class="block hover:text-gray-600">Accueil</a>
        <hr>
        <a href="{{ path('app_login') }}" class="block hover:text-gray-600">Connexion</a>
        <hr>
        <a href="{{ path('app_register') }}" class="block hover:text-gray-600">Inscription</a>
    </div>
</nav>

<script>
	document.getElementById('menu-button').addEventListener('click', function() {
		document.getElementById('mobile-menu').classList.toggle('hidden');
	});
</script>
